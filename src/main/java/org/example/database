create table photo
(
    id   bigserial
        constraint photo_pk
            primary key,
    date bytea not null
);

alter table photo
    owner to postgres;

    create table photo
    (
        id   bigserial not null
            constraint photo_pk
                primary key,
        date oid       not null
    );